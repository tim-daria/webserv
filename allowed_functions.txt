accept (прием соединения)

access (проверка прав доступа к файлу)

bind (привязка сокета к адресу/порту)

chdir (смена рабочей директории)

close (закрытие дескриптора)

closedir (закрытие потока директории)

connect (подключение к серверу)

dup (дублирование дескриптора)

dup2 (дублирование дескриптора в конкретный номер)

epoll_create (создание инстанса epoll)

epoll_ctl (управление событиями в epoll)

epoll_wait (ожидание событий в epoll)

errno (переменная кода последней ошибки)

execve (запуск программы — критично для CGI)

fcntl (манипуляция флагами дескриптора, например, O_NONBLOCK)

fork (создание дочернего процесса)

freeaddrinfo (освобождение памяти после getaddrinfo)

gai_strerror (текстовая ошибка для getaddrinfo)

getaddrinfo (получение сетевого адреса по имени хоста)

getprotobyname (получение протокола по имени)

getsockname (получение адреса сокета)

htonl (host to network long)

htons (host to network short)

kevent (регистрация/ожидание событий в kqueue)

kill (отправка сигнала процессу)

kqueue (создание очереди событий ядра)

listen (перевод сокета в режим прослушивания)

ntohl (network to host long)

ntohs (network to host short)

open (открытие файла)

opendir (открытие директории)

pipe (создание канала данных — нужно для CGI)

poll (ожидание событий на дескрипторах)

read (чтение данных)

readdir (чтение содержимого директории)

recv (получение данных из сокета)

select (синхронное мультиплексирование ввода-вывода)

send (отправка данных в сокет)

setsockopt (настройка параметров сокета, например, SO_REUSEADDR)

signal (обработка сигналов)

socket (создание сокета)

socketpair (создание пары соединенных сокетов)

stat (получение информации о файле)

strerror (перевод кода ошибки в строку)

waitpid (ожидание завершения дочернего процесса)

write (запись данных)
