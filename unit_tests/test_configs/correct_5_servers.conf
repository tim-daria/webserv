# server 1
server {
    listen 8080;
    server_name 1;

    root ./www;
    location / {
			limit_except GET POST;
    }
}

# server 2
server {
    listen 8081;
    server_name 2;

    root ./www;
    location / {
			limit_except GET POST;
    }
}

# server 3
server {
    listen 8083;
    server_name 3;

    root ./www;
    location / {
			limit_except GET POST;
    }
}

# server 4
server {
    listen 8084;
    server_name 4;

    root ./www;
    location / {
			limit_except GET POST;
    }
}

# server 5
server {
    listen 8085;
    server_name 5;

    root ./www;
    location / {
			limit_except GET POST;
    }
}
